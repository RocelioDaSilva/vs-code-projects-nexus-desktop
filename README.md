# vs code projects nexus desktop

This repository was initialized from the workspace at `C:\Users\PCGAME\Desktop\Master VS CODE PROJECTS`.

**Repository name:** vs code projects nexus desktop

Contents mirror the workspace root. To add a remote GitHub repository and push, you can run:

```
# create a GitHub repo interactively using the GitHub CLI
gh repo create "vs-code-projects-nexus-desktop" --public
# or add a remote and push
git remote add origin <remote-url>
git push -u origin main
```
